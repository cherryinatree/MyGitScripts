using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MonoBehaviourSave : MonoBehaviour
{
    /*
    protected void SaveMyDataStart(Item myData)
    {
        if (string.IsNullOrEmpty(myData.id))
        {
            myData.id = System.DateTime.Now.ToLongDateString() + System.DateTime.Now.ToLongTimeString() + Random.Range(0, int.MaxValue).ToString();
            SaveData.Current.mainData.itemInventory.Add(myData);
        }
    }

    protected void SaveMyData(SaveObject myData)
    {
        for (int i = 0; i < SaveData.Current.objects.Count; i++)
        {
            if (SaveData.Current.objects[i].id == myData.id)
            {
                SaveData.Current.objects[i] = myData;
            }
        }
    }
    */

}
