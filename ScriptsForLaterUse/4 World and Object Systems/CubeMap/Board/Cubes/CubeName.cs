using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//[ExecuteInEditMode]
public class CubeName : MonoBehaviour
{
    private static int cubeNumber = 0;
    // Start is called before the first frame update
    private void Awake()
    {
        //cubeNumber = 0;
    }


    void Start()
    {
        //cubeNumber++;
        //gameObject.name = "Cube"+cubeNumber.ToString();
    }

}
